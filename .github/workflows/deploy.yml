name: Deploy

on:
  push:
    branches:
      - '**'

jobs:
  deploy:
  runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Update qc-payroll.goldstore.id
        run: |
          cd /var/www/html/qc/qc-payroll.goldstore.id
          git fetch origin "$GITHUB_REF_NAME"
          git reset --hard origin/"$GITHUB_REF_NAME"
          git pull origin "$GITHUB_REF_NAME"

      - name: Update qc-payroll-cabang.goldstore.id
        run: |
          cd /var/www/html/qc/qc-payroll-cabang.goldstore.id
          git fetch origin "$GITHUB_REF_NAME"
          git reset --hard origin/"$GITHUB_REF_NAME"
          git pull origin "$GITHUB_REF_NAME"

      - name: Build Selesai
        run: echo "✅ Build berhasil!"